# Small like as angular implementations on ts


project have:  
- basic implementation of a Router for lazy loading of the first module

- @HostBinding decorator for components - bind values and class on "host" of component
